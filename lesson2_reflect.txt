What happens when you initialize a repository? Why do you need to do it?

a .git folder is created. it is needed to record and track changes in the repository

How is the staging area different from the working directory and the repository? What value do you think it offers?

staging area is the go between the two. we can choose which files to commit via the staging area..........

How can you use the staging area to make sure you have one commit per logical change?

by using git diff


What are some situations when branches would be helpful in keeping your history organized? How would branches help?

in experimenting with new features when collaborating with others on a project.
one can switch when one gets bored working on a feature to another feature.
one can create a branch to solve a bug so that you can fully concentrate on bug solving.

How do the diagrams help you visualize the branch structure?
it shows how and what features/bugs are being worked on

What is the result of merging two branches together? Why do we represent it in the diagram the way we do?
we can merge two features easily....diagram to clearly show the commits in each branch.

What are the pros and cons of Git’s automatic merging vs. always doing merges manually?
pros --it's being done automatically
cons -- many time the merged code will not make sense

